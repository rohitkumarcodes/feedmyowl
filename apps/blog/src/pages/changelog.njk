---
layout: layouts/base.njk
title: changelog — feedmyowl
permalink: /changelog/
---
{# Changelog index as a quiet vertical stream: title, date, and excerpt. #}

<section class="stream-index">
  <h1>Changelog</h1>

  {% set changelogEntries = collections.changelog | reverse %}
  {% if changelogEntries.length %}
    <ul class="stream-list">
      {% for entry in changelogEntries %}
        <li class="stream-item">
          <a href="{{ entry.url }}" class="stream-link">{{ entry.data.title }}</a>
          <p class="stream-meta">{{ entry.date | readableDate }}</p>
          <p class="stream-excerpt">
            {{ (entry.data.description or entry.templateContent | striptags) | truncate(280, true, "…") }}
          </p>
        </li>
      {% endfor %}
    </ul>
  {% else %}
    <p>No changelog entries yet.</p>
  {% endif %}
</section>
