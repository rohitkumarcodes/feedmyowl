/**
 * Settings Page — Account & Subscription Management
 *
 * This page lets the user:
 *   1. View their account info
 *   2. Manage their subscription (upgrade to paid, manage billing)
 *   3. Export their data (OPML export)
 *   4. Delete their account
 *
 * Currently a placeholder that will be built out with actual settings UI.
 */

export default function SettingsPage() {
  return (
    <div>
      <h1>Settings</h1>
      <p style={{ marginTop: "1rem", color: "#666" }}>
        Account and subscription settings will appear here.
      </p>

      {/* TODO: Account info section */}
      {/* TODO: Subscription management (upgrade / manage billing) */}
      {/* TODO: OPML export */}
      {/* TODO: Delete account */}
    </div>
  );
}
