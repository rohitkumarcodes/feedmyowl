"use client";

/**
 * Client-safe auth UI exports.
 *
 * This module is used by client components that need Clerk UI primitives.
 * Keep server-only auth APIs in "@/lib/auth".
 */
export { UserButton as UserMenu } from "@clerk/nextjs";
